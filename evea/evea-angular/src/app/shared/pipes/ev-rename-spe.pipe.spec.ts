import { EvRenameSpePipe } from './ev-rename-spe.pipe';

describe('EvRenameSpePipe', () => {
  it('create an instance', () => {
    const pipe = new EvRenameSpePipe();
    expect(pipe).toBeTruthy();
  });
});
