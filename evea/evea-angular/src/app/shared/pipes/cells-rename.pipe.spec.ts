import { CellsRenamePipe } from './cells-rename.pipe';

describe('CellsRenamePipe', () => {
  it('create an instance', () => {
    const pipe = new CellsRenamePipe();
    expect(pipe).toBeTruthy();
  });
});
